# Copier en .env.local (gitignoré).
# URL du backend (V3.6). Vide/absent → le site reste statique (contenu = content/*.json).
NEXT_PUBLIC_API_URL=https://api.exemple.tld

# URL publique du site (SEO V3.7 — métadonnées / sitemap).
NEXT_PUBLIC_SITE_URL=https://exemple.tld
